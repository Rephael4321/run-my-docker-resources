# Make file executable
chmod +x <file-name>

# Execute command with auto OK
yes | ./instal-docker-docker-compose.sh

# Check Docker version
docker -v

# Check Docker Compose version
docker compose version
